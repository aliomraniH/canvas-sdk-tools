"""canvas-sdk-tools — Tier-3 Canvas-only MCP.

Static, offline, stateless analyzers that validate Canvas SDK plugin code and
specs against vendored, version-pinned reference data.  This package never calls
a Canvas instance, holds no Canvas credentials, and keeps no per-request state.
"""

__version__ = "0.1.0"
